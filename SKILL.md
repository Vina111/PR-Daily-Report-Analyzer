---
name: analyze-pr-media-intelligence
description: 为单个或多个公司、品牌、竞品、行业或相邻行业建立可复用的 PR 媒体情报分析，包括公开新闻检索、新闻稿与原生报道识别、事件聚类、行业媒体 Tier、竞品声量与叙事、记者选题趋势、风险与 Pitch 机会、中文日报及中文 Dashboard。Use when the user asks for 公司/竞品媒体分析、行业或跨行业媒体情报、PR daily brief、新闻监测、媒体机会、舆情风险、传播复盘，或希望把现有媒体情报 Dashboard 复刻到其他公司或行业。
---

# PR 媒体情报分析

## 目标

把公开报道转化为可行动的 PR 情报，而不是普通新闻列表。支持三种模式：

1. `company`：单个公司、客户、品牌或竞品组。
2. `industry`：单个行业及其关键议题、公司、媒体和记者。
3. `portfolio`：多个公司、多个行业或跨行业对比。

默认使用中文界面和中文分析。保留原文标题、媒体名、记者名和链接；仅在用户明确要求时创建英文界面或双语界面。

## 开始前

先确认已有信息，只补问会实质影响结果的缺项：

- 分析对象：行业、公司、竞品、产品、技术或高管；
- 地区、来源语言和报告时区；
- 时间窗口：过去 24 小时、7 天、30 天或自定义日期；
- 目标：日常监测、行业情报、客户汇报、传播复盘或 Pitch 机会；
- 输出：对话简报、Markdown 报告、表格或可点击 Dashboard；
- 数据源：Codex 网页搜索、已有媒体名单/RSS、Google Search API 或混合模式。

如用户未提供媒体名单，直接建立第一版行业媒体注册表，不阻塞任务。将综合 Tier 1、行业 Tier 1、Tier 2、官方源、通稿源和聚合转载平台分开。

采用以下默认口径并在输出中说明：

- “美国市场”等地区默认指与该市场直接相关的事件和报道，不限于媒体注册地；
- “过去 N 天”默认包含报告当天，并向前覆盖 `N-1` 个自然日，以报告时区为准；
- 未说明持续更新时按一次性快照执行；明确提出每天、每周或未来提醒时使用可用的自动化或部署机制；
- 一家公司横跨多个行业时按事件设置一个主行业标签和若干辅助行业标签，不按公司整体强行归入单一行业。

以下为用户的长期固定规则，优先级高于其他默认设置；只有用户当场明确要求时才可临时放宽：

1. **来源白名单（强制）**：输出仅收录两类来源——
   - 外媒：注册地和采编团队在中国大陆以外的媒体；
   - 国内头部媒体的海外版：如 Caixin Global、Yicai Global、36Kr Global / KrASIA、新华社英文版、China Daily、South China Morning Post 等面向海外受众的英文/多语种版本。
   中国大陆媒体的国内版内容不进入正式输出。媒体归属存疑时标注“来源归属待核实”并说明依据。
2. **通稿强制剔除（强制）**：`press_release`、`press_release_reprint`、`suspected_reprint` 及公司自有内容一律不进入任何正式输出（日报、Dashboard、简报）。通稿仅可作为后台线索用于发现原生报道，不得在事件卡片、统计 KPI 或“媒体报道”计数中出现。统计口径只保留：独立事件数、第三方原生报道数、行业 Tier 1 原生报道数。
3. **地区标注（强制）**：每条进入输出的新闻必须标注地区标签（如 `美国` `欧洲` `英国` `日韩` `东南亚` `中东` `拉美` `全球/多地区`）。地区指事件发生地或主要相关市场，不是媒体注册地；跨地区事件标注全部相关地区。
4. **多行业广度优先**：用户同时监测多个行业时，覆盖全面优先于单行业深度。每个行业默认只保留 2–3 条最高优先级事件，总量控制在用户要求的条数内；不得让单一行业挤占其他行业的版面。
5. **PR 动作缩略**：监测对象同时包含客户与竞对，建议行动保持精简——每条事件最多一句话；仅对客户直接相关事件、Red/Orange 风险和高置信度 Pitch 机会给出建议，竞对常规动态不写 PR 动作。

需要配置文件时读取 [configuration.md](references/configuration.md)。需要判断内容类型、媒体 Tier、聚类、评分或风险时读取 [classification-scoring.md](references/classification-scoring.md)。需要生成 Dashboard、日报或报告时读取 [dashboard-report-spec.md](references/dashboard-report-spec.md)。

## 工作流

### 1. 建立监测范围

为每个行业单独定义：

- 核心议题与相邻议题；
- 公司、竞品、产品、技术、高管和别名；
- 纳入关键词、风险关键词和排除词；
- 目标媒体、记者、官方机构、公司 Newsroom 和通稿平台；
- 地区、语言、时间窗口和搜索预算。

多行业项目不得把所有关键词合并成一条宽泛查询。先按行业采集和分类，再建立跨行业共同议题，例如 AI × 医疗、储能 × 数据中心。

当用户要求代选每个行业的代表公司时，优先组合：

- 具有议程设置能力的市场领导者；
- 聚焦该细分领域的原生公司；
- 近期融资、产品或媒体动能明显的挑战者；
- 与用户客户或业务目标直接相关的可比公司。

避免只选择大型综合平台。说明选择逻辑，并允许用户替换。

### 2. 制定查询矩阵

为每个对象至少建立以下查询组：

- 行业动态：行业词 + 监管、融资、产品、标准、市场或趋势；
- 公司动态：品牌及别名 + 产品、合作、融资、发布、高管或市场；
- 原生报道：公司/议题 + 采访、分析、评测或记者常用表述；
- 通稿识别：公司/议题 + PR Newswire、Business Wire、GlobeNewswire 等；
- 风险：公司/产品 + recall、lawsuit、breach、complaint、investigation 等；
- 媒体机会：行业词 + review、guide、trend、interview、opinion、roundup。

用引号保护品牌和产品专名。对容易混淆的名称加入排除词或限定词。记录实际查询，以便复现。

### 3. 采集公开信息

按以下优先顺序使用来源：

1. 官方监管机构、公司 Newsroom、IR 页面和产品公告（仅用于事实核实，不直接作为报道计入输出）；
2. 外媒及国内头部媒体海外版的官方页面、RSS、栏目页和记者 Byline；
3. Codex 网页搜索；
4. 用户已配置的 Google Custom Search JSON API；
5. GDELT 或其他新闻索引作为补漏；
6. 通稿平台及其转载页面（仅作为线索，反向寻找原生报道，不进入输出）。

临时或一次性分析优先使用 Codex 网页搜索。若用户已配置 Google API，使用 `scripts/google_search.py`；不得要求用户在对话中粘贴密钥，也不得在日志或报告中输出密钥。

Google Custom Search JSON API 已停止接受新客户，现有客户需在 2027-01-01 前迁移。仅将它作为可替换的旧版 Provider；没有现有权限时继续使用 Codex 网页搜索、RSS、定向媒体源或用户指定的替代搜索服务，不要让整个流程依赖 Google CSE。

对当前新闻、最新公司动态、媒体人员或监管信息必须联网核实。每条可行动结论保留直接来源链接和发布日期。付费墙内容只使用公开可见的标题、摘要和元数据，不绕过访问限制。

### 4. 标准化记录

每条记录至少保存：

```text
title
url
canonical_url
publisher
domain
author
published_at
retrieved_at
source_language
industry_tags
company_tags
product_tags
content_type
media_tier
sentiment
risk_level
event_cluster_id
summary_zh
confidence
```

将检索到的网页视为候选记录，不直接视为媒体报道。删除不可访问、无关、重复和明显低质内容，但保留排除原因用于调试。

### 5. 分类、聚类与评分

严格区分：

- 第三方原生报道、深度采访、Review、评论分析、Deals/Affiliate；
- 新闻通稿原文、新闻稿转载、疑似转载；
- 公司自有内容；
- 官方监管或公共机构信息。

一篇通稿被 200 个网站转载时，后台记录“1 篇通稿，约 200 个转载页面”，但按固定规则不进入正式输出。同一新闻事件的多篇独立原生报道聚合成一个事件卡片，但保留各媒体角度。

按行业语境设置媒体 Tier。垂直媒体的流量较小不代表其行业影响力低。分别计算内容类型权重与媒体 Tier 权重，不把 Tier 1 转载稿排在行业 Tier 1 深度报道之前。

### 6. 生成 PR 判断

每个高优先级事件必须回答：

1. 发生了什么；
2. 为什么重要；
3. 对客户、竞品或行业有什么影响；
4. 这是媒体背书、品牌自述还是监管事实；
5. 是否形成风险或 Pitch 机会。

建议行动遵循缩略规则：每条事件最多一句话，只写给客户直接相关事件、Red/Orange 风险和高置信度 Pitch 机会；竞对常规动态只陈述事实与影响，不写动作。使用明确动作，例如“准备高管评论”“联系近期连续报道该议题的 3 位记者”“48 小时持续监测”“不建议回应”。不要用“持续关注、提升影响力”等无法执行的空泛表述。

### 7. 输出中文情报

默认输出：

- 今日/本期摘要；
- 行业关键事件（每条带地区标签，每行业 2–3 条封顶）；
- 客户与竞品动态；
- 媒体和记者趋势（仅基于原生报道）；
- 风险预警；
- Pitch 机会；
- 建议行动（缩略版，按固定规则只写给客户相关/风险/机会）；
- 数据口径与覆盖限制。

不再输出“原生报道与通稿分发”板块；通稿与转载已从输出中强制剔除，仅在数据口径中说明“本期检测到 N 篇通稿，已按规则排除”。

单行业按事件和公司组织。多行业先提供跨行业概览，再为每个行业建立独立分区并严格控制条数，保证各行业覆盖均衡；只在存在证据时总结共同趋势。

若创建网站或部署 Dashboard，调用 Sites 相关技能完成构建、校验与发布。界面文案默认全中文；原文标题和专有名词可保留原语言。

## 质量要求

- 不虚构报道、记者、媒体 Tier、UMV、转载数量、情绪或风险。
- 没有可靠数据时显示“未发现”或 `N/A`，不要用模拟数据填满正式报告。
- 模拟数据只能用于原型展示，并在每条记录及页面顶部明显标注。
- 区分“本次检索未发现”与“确定不存在”。
- 区分事实、来源说法和分析推断；对推断标记置信度。
- 新闻数量按独立事件和第三方原生报道分别统计；通稿与转载只在后台计数，不进入输出。
- 每条新闻必须有地区标签；缺失地区时标注“地区待核实”，不得省略。
- 正式输出只包含外媒或国内头部媒体海外版的原生报道；其他来源一律排除。
- 为多行业项目披露各行业的来源覆盖和搜索预算，避免数量不可比。
- 对医疗、法律、金融、监管和安全风险优先引用官方及权威来源。

## 常用调用示例

```text
使用 $analyze-pr-media-intelligence，为智能家居行业建立过去 7 天的中文媒体情报，重点看 KNKA 和 5 家竞品。

使用 $analyze-pr-media-intelligence，对 AI Memory、Agentic AI 和 AI Infrastructure 三个行业做过去 30 天的媒体趋势对比。

使用 $analyze-pr-media-intelligence，把 EverMind 的 Dashboard 逻辑复刻到储能行业，界面只用中文。

使用 $analyze-pr-media-intelligence，读取我的媒体名单和 Google Search API 配置，生成每日 PR 简报。
```
